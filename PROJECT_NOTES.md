# Bartender — תיעוד פרויקט תפריט דיגיטלי

> עודכן לאחרונה: 2026-03-13

---

## מה בנינו

אפליקציית תפריט אינטראקטיבית חד-עמודית (Single Page App) לפאב **Bartender**.
כל הקוד בשני קבצים בתיקיית `PUB/`:

| קובץ | תפקיד |
|------|--------|
| `menu.html` | מבנה, עיצוב (CSS), ולוגיקה (JS) |
| `data.js` | **כל התוכן הניתן לעריכה** — מערכי נתונים |

---

## מסכים (8 מסכים)

| מסך | תיאור |
|-----|--------|
| 🏠 **דף פתיחה** | לוגו, כותרת BARTENDER, ניווט לשאר המסכים |
| ℹ️ **על הבר** | תיאור הפאב |
| 👥 **הצוות** | כרטיסי הצוות עם תמונה, שם, תפקיד, ביו |
| 🥤 **שתייה קלה** | רשימת פריטים עם ניקובים ומחיר |
| 🥃 **שתייה חריפה** | קטגוריות + פריטים |
| ⭐ **ספיישל היום** | פריט יומי בולט |
| 📅 **אירועי החודש** | רשימת אירועים |
| 🍽️ **אוכל** | מנות עם ניקובים |
| 📸 **תמונות** | גלריה (תמונה1.jpg / תמונה2.jpg / תמונה3.jpg) |

---

## מערכת הניקובים (כרטיס פאנץ')

- כרטיס = **100 ₪ → 20 ניקובים**
- 1 ניקוב = 5 ₪
- כל פריט מציג: מספר ניקובים + מחיר ב-₪

---

## פיצ'רים

- **❤️ לייק** — שמור ב-`localStorage` (מפתח: `btdr_likes`)
- **🆕 תג NEW** — מוצג על פריטים חדשים; נעלם לאחר כניסה לראשונה (מפתח: `btdr_seen`)
- **עברית / English** — כפתור החלפת שפה בדף הפתיחה (מפתח: `btdr_lang`)
  - RTL בעברית, LTR באנגלית
  - כל הפריטים ב-`data.js` כוללים שדות `nameEn`, `descEn`, `roleEn`, `bioEn`, `titleEn`, `tagEn`, `dateEn`
- **🎵 מוזיקת רקע** — ג'אז לאונג' אמביינט שנוצר ב-Web Audio API (ללא קבצים חיצוניים)
  - מתחיל בנגיעה ראשונה (דרישת הדפדפן)
- **מעברי מסך חלקים** — `translateX` + cubic-bezier

---

## עריכת תוכן — רק ב-`data.js`

```
data.js
├── TEAM         ← אנשי הצוות
├── SOFT         ← שתייה קלה
├── SPIRITS      ← שתייה חריפה (כולל כותרות קטגוריות)
├── SPIRITS_EN   ← תרגום כותרות קטגוריות לאנגלית
├── SPECIAL_HERO ← ספיישל היום
├── EVENTS       ← אירועי החודש
└── FOOD         ← אוכל
```

**דוגמה לעריכת פריט:**
```js
{ id: 'so1', name: 'קולה', nameEn: 'Cola', punches: 1, desc: 'קלאסיק', descEn: 'Classic', tag: 'NEW', tagEn: 'NEW' }
```

**דוגמה לעריכת ספיישל היום:**
```js
const SPECIAL_HERO = {
  name: 'שם הפריט', nameEn: 'Item Name',
  desc: 'תיאור בעברית', descEn: 'Description in English',
  punches: 2,
};
```

---

## אירוח — Netlify Drop

1. גרור את **תיקיית PUB** לכתובת `netlify.com/drop`
2. מקבלים לינק ציבורי מיידית
3. שלח בוואטסאפ

> ⚠️ **בעיה ידועה:** קבצים עם שמות בעברית (`תמונה1.jpg`) עלולים לא לעבוד ב-Netlify.
> פתרון: שנה שמות ל-`photo1.jpg`, `photo2.jpg`, `photo3.jpg` ועדכן ב-`menu.html`.

---

## פתיחה מקומית (בדיקה מהמחשב)

כשפותחים `menu.html` ישירות מהמחשב (פרוטוקול `file://`) —
הדפדפן **חוסם טעינת** `data.js` החיצוני → המסכים יופיעו ריקים.

**פתרון:** להשתמש תמיד דרך Netlify (HTTP), לא ישירות מהקובץ.

---

## קבצי תמונות בתיקייה

| קובץ | שימוש |
|------|--------|
| `publogo.jpg` | לוגו הפאב (דף פתיחה) |
| `תמונה1.jpg` | גלריה |
| `תמונה2.jpg` | גלריה |
| `תמונה3.jpg` | גלריה |

---

## הערות טכניות

- הלוגו מוצג עם `filter: brightness(0) invert(1) sepia(1)...` במקום `mix-blend-mode: screen`
  (פותר בעיית Safari iOS עם transform context)
- כותרת ראשית: `BARTENDER` טקסט, לא תמונה
- כל המסכים ממוקמים absolute עם z-index 2; דף הבית z-index 1
- הגלריה לא מבוססת על `data.js` — לשינוי תמונות יש לערוך ישירות ב-`menu.html`
